vInicial = 150; // Velocidade no início
desaceleracao = 20; // Quanto diminui por segundo
t = Math.max(0, time); // Garante que o tempo não seja negativo

// Fórmula: v0*t - (a*t^2)/2
(vInicial * t) - (desaceleracao * Math.pow(t, 2) / 2);
