var startAmount = 30; 
var decayRate = 10;   

var dynamicAmount = Math.max(startAmount - (time * decayRate), 0);

// Em Rotation, o wiggle já retorna o valor final direto
wiggle(0.5, dynamicAmount);
